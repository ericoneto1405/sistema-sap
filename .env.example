# =============================================================================
# CONFIGURAÇÃO DO AMBIENTE - Sistema SAP
# =============================================================================
# 
# Copie este arquivo para .env e preencha com suas configurações:
#   cp .env.example .env
#
# ⚠️ NUNCA commite o arquivo .env no Git!
# =============================================================================

# -----------------------------------------------------------------------------
# AMBIENTE
# -----------------------------------------------------------------------------
# Valores possíveis: development, testing, production
FLASK_ENV=development

# Habilitar debug (NUNCA em produção!)
FLASK_DEBUG=True

# Host e porta do servidor de desenvolvimento
HOST=0.0.0.0
PORT=5004

# -----------------------------------------------------------------------------
# SEGURANÇA
# -----------------------------------------------------------------------------
# SECRET_KEY: Use uma chave aleatória forte de no mínimo 32 caracteres
# Gere uma com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=sua-secret-key-super-segura-min-32-caracteres-aqui

# -----------------------------------------------------------------------------
# BANCO DE DADOS
# -----------------------------------------------------------------------------
# Desenvolvimento (SQLite):
DATABASE_URL=sqlite:///instance/sistema.db

# Produção (PostgreSQL):
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/sistema_sap

# Produção (MySQL):
# DATABASE_URL=mysql+pymysql://usuario:senha@localhost:3306/sistema_sap

# -----------------------------------------------------------------------------
# REDIS (Cache e Rate Limiting)
# -----------------------------------------------------------------------------
# Deixe vazio para usar cache em memória (apenas desenvolvimento)
REDIS_URL=

# Produção (Redis):
# REDIS_URL=redis://localhost:6379/0

# Com senha:
# REDIS_URL=redis://:senha@localhost:6379/0

# Redis Cloud:
# REDIS_URL=redis://default:senha@redis-xxxxx.cloud.redislabs.com:12345

# -----------------------------------------------------------------------------
# GOOGLE VISION (OCR)
# -----------------------------------------------------------------------------
# Caminho para o arquivo de credenciais JSON do Google Cloud
GOOGLE_APPLICATION_CREDENTIALS=/caminho/para/gvision-credentials.json

# Limite mensal de chamadas OCR
OCR_MONTHLY_LIMIT=1000

# Forçar limite de OCR (True/False)
OCR_ENFORCE_LIMIT=True

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
# Nível de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# SESSÃO
# -----------------------------------------------------------------------------
# Tempo de vida da sessão em segundos (28800 = 8 horas)
PERMANENT_SESSION_LIFETIME=28800

# -----------------------------------------------------------------------------
# RATE LIMITING
# -----------------------------------------------------------------------------
# Habilitar rate limiting (True/False)
RATELIMIT_ENABLED=True

# Limites padrão (formato: "X per day; Y per hour")
RATELIMIT_DEFAULT=200 per day;50 per hour

# -----------------------------------------------------------------------------
# SMTP (Email) - OPCIONAL
# -----------------------------------------------------------------------------
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=seu-email@gmail.com
# MAIL_PASSWORD=sua-senha-de-app
# MAIL_DEFAULT_SENDER=noreply@sistema-sap.com

# -----------------------------------------------------------------------------
# SENTRY (Monitoramento de Erros) - OPCIONAL
# -----------------------------------------------------------------------------
# SENTRY_DSN=https://xxxxx@sentry.io/xxxxx
# SENTRY_ENVIRONMENT=production
# SENTRY_TRACES_SAMPLE_RATE=0.1

# -----------------------------------------------------------------------------
# AWS S3 (Storage) - OPCIONAL
# -----------------------------------------------------------------------------
# AWS_ACCESS_KEY_ID=sua-access-key
# AWS_SECRET_ACCESS_KEY=sua-secret-key
# AWS_S3_BUCKET=sistema-sap-uploads
# AWS_S3_REGION=us-east-1

# =============================================================================
# FIM DA CONFIGURAÇÃO
# =============================================================================
